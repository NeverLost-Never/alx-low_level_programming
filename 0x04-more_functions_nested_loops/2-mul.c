#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: number input
 * @b: number input
 * Return: 1 if c is a digit, otherwise 0
 */
int mul(int a, int b)
{
	return (a * b);
}
